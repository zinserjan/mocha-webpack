/* eslint-disable */

var assert = require('assert');
var lazyLoadEntry = require('../src/lazy-load-none');

describe('lazy-load-entry-static', function () {
  it('runs test', function (cb) {
    lazyLoadEntry(cb);
  });
});
